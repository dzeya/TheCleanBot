{
  "version": 2,
  "builds": [
    { "src": "api/webhook.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "src": "/api/bot-webhook", "dest": "api/webhook.py" }
  ],
  "installCommand": "pip install -r requirements.txt"
} 